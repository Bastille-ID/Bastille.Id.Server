@using Bastille.Id.Models
@model Bastille.Id.Server.Core.Security.Models.ManageUserProfileModel
@{
    ViewData["Title"] = Resources.ProfileSettingsText;
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section Head {
    <style>
        .bd-placeholder-img {
            font-size: 1.125rem;
            text-anchor: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            user-select: none;
        }

        @@media (min-width: 768px) {
            .bd-placeholder-img-lg {
                font-size: 3.5rem;
            }
        }
    </style>
}

@section scripts {
    <script type="text/javascript">
        //siteApp.notifyPrompt("@Html.Raw(Model.StatusMessage)", "@Model.StatusType", @((Model.StatusType == StatusMessageResultType.Success).ToString().ToLower()));
    </script>
}